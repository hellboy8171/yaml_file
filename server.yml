---
  - name: Installing HTTPD Server
    hosts: all
    become_user: ansible
    gather_facts: no
    tasks: 
      - name: installing httpd server
        yum: 
          name: httpd
          state: latest
